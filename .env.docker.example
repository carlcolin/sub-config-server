PORT=3210
HOST=0.0.0.0
ACCESS_TOKEN=***
# 多 token 可用英文逗号分隔，配置后会覆盖单个 ACCESS_TOKEN
# ACCESS_TOKENS=token1,token2
CONFIG_DIR=./configs
ROUTES_FILE=./routes.json
TRUST_PROXY=false
# 若前面有 Nginx / Caddy / Cloudflare 等反代，可改为 true
# TRUST_PROXY=true
# 可选：用于启动日志和 /profiles 返回更实用的公网/域名地址
# PUBLIC_BASE_URL=https://sub.example.com
